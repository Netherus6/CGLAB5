

| **实验名称** | 光线追踪 |
| :--- | :--- |
| **授课教师** | 张鸿文 |
| **助教** | 张怡冉 |
| **姓名** | 任炳瑞 |
| **学号** | 202311081001 |
| **日期** | 2026年6月12日 |




---

## 一、实验目标
1. **理论理解**：理解光线投射（Ray Casting）与光线追踪（Ray Tracing）的本质区别
2. **全局光照**：掌握如何通过发射“次级射线”来实现硬阴影和理想镜面反射
3. **GPU编程思维**：学习将传统的“递归”光线追踪算法改写为适合GPU并行计算的“迭代”模式

---

## 二、实验原理
### 2.1 Whitted-Style光线追踪模型
本实验采用经典的Whitted-Style光线追踪模型。其核心思想是：当一条主光线从摄像机出发击中物体表面时，根据表面材质属性，可能产生以下两种次级射线：

1. **阴影射线**：从交点向光源方向发射，用于判断该点是否处于阴影中
2. **反射射线**：对于镜面材质，根据反射定律产生新的射线继续传播

### 2.2 反射向量计算
反射向量的计算公式为：

$ \mathbf{R} = \mathbf{L}_{in} - 2(\mathbf{L}_{in} \cdot \mathbf{N})\mathbf{N} $

其中：

+ $ \mathbf{R} $ 为反射方向
+ $ \mathbf{L}_{in} $ 为入射光线方向
+ $ \mathbf{N} $ 为表面法向量

### 2.3 递归转迭代
由于GPU不擅长处理递归调用，本实验将递归算法改写为迭代循环：

```plain
for bounce in range(max_bounces):
    求交计算
    if 击中镜面材质:
        更新射线起点和方向
        更新吞吐量(throughput)
        继续循环
    else if 击中漫反射材质:
        计算阴影和光照
        累加颜色
        跳出循环
```

---

## 三、实验环境
| 项目 | 配置 |
| :--- | :--- |
| 操作系统 | Windows |
| 编程语言 | Python 3.13 |
| GPU框架 | Taichi 1.7.4 |
| 后端 | Vulkan |
| 集成环境 | PyCharm |


---

## 四、实验内容与实现
### 4.1 场景搭建
实验场景包含三个几何体：

| 物体 | 类型 | 位置 | 半径 | 材质 | 颜色 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 红色球体 | 球体 | (-1.5, 0.0, 0.0) | 1.0 | 漫反射 | (0.8, 0.1, 0.1) |
| 银色球体 | 球体 | (1.5, 0.0, 0.0) | 1.0 | 理想镜面 | (0.9, 0.9, 0.9) |
| 地面 | 无限大平面 | y = -1.0 | - | 漫反射 | 黑白棋盘格 |


**棋盘格纹理实现**：通过交点 $ (x, z) $ 坐标判断格子颜色

```python
ix = ti.floor(p.x * grid_scale)
iz = ti.floor(p.z * grid_scale)
if (ix + iz) % 2 == 0: color = COLOR1
else: color = COLOR2
```

### 4.2 光线与物体求交
#### 球体求交
射线方程：$ \mathbf{P}(t) = \mathbf{O} + t\mathbf{D}, \quad t > 0 $

代入球体方程：$ |\mathbf{P} - \mathbf{C}|^2 = R^2 $，得到一元二次方程：

$ t^2(\mathbf{D} \cdot \mathbf{D}) + 2t(\mathbf{D} \cdot (\mathbf{O} - \mathbf{C})) + (\mathbf{O} - \mathbf{C}) \cdot (\mathbf{O} - \mathbf{C}) - R^2 = 0 $

判别式 $ \Delta = b^2 - 4ac $，当 $ \Delta > 0 $ 时存在两个交点，取最小的正 $ t $ 值。

#### 平面求交
平面方程：$ y = y_0 $，代入射线方程：

$ O_y + tD_y = y_0 \Rightarrow t = \frac{y_0 - O_y}{D_y} $

### 4.3 阴影实现
从交点向光源发射阴影射线，判断是否被其他物体遮挡：

```python
shadow_ray_orig = p + N * EPSILON  # 偏移防止自相交
shadow_t, _, _, _ = scene_intersect(shadow_ray_orig, L)
dist_to_light = (light_pos - p).norm()

if shadow_t < dist_to_light:
    in_shadow = True  # 处于阴影中
```

### 4.4 光照计算
对于漫反射表面，采用Phong模型中的环境光和漫反射光分量：

$ I = I_a + I_d = k_a \cdot C + k_d \cdot \max(0, \mathbf{N} \cdot \mathbf{L}) \cdot C $

其中 $ k_a = 0.2 $ 为环境光系数，$ k_d = 0.8 $ 为漫反射系数。

### 4.5 镜面反射与光线衰减
击中镜面材质时：

1. 计算反射方向：$ \mathbf{R} = \mathbf{L}_{in} - 2(\mathbf{L}_{in} \cdot \mathbf{N})\mathbf{N} $
2. 更新射线起点（法线偏移）
3. 更新吞吐量：`throughput *= 0.8 * obj_color`
4. 继续下一次迭代

### 4.6 交互界面
使用 `ti.ui.Window` 创建控制面板，提供以下实时调节参数：

| 参数 | 范围 | 默认值 | 说明 |
| :--- | :--- | :--- | :--- |
| Light X | -5.0 ~ 5.0 | 2.0 | 光源X坐标 |
| Light Y | 1.0 ~ 8.0 | 4.0 | 光源Y坐标 |
| Light Z | -5.0 ~ 5.0 | 3.0 | 光源Z坐标 |
| Max Bounces | 1 ~ 5 | 3 | 最大光线弹射次数 |


---

## 五、核心代码
### 5.1 场景求交函数
```python
@ti.func
def scene_intersect(ro, rd):
    min_t = 1e10
    hit_n = ti.Vector([0.0, 0.0, 0.0])
    hit_c = ti.Vector([0.0, 0.0, 0.0])
    hit_mat = MAT_DIFFUSE
    
    # 红色漫反射球
    t, n = intersect_sphere(ro, rd, ti.Vector(RED_SPHERE_CENTER), RED_SPHERE_RADIUS)
    if 0 < t < min_t:
        min_t, hit_n, hit_c, hit_mat = t, n, ti.Vector(RED_SPHERE_COLOR), MAT_DIFFUSE
    
    # 银色镜面球
    t, n = intersect_sphere(ro, rd, ti.Vector(MIRROR_SPHERE_CENTER), MIRROR_SPHERE_RADIUS)
    if 0 < t < min_t:
        min_t, hit_n, hit_c, hit_mat = t, n, ti.Vector(MIRROR_SPHERE_COLOR), MAT_MIRROR
    
    # 地面
    t, n = intersect_plane(ro, rd, GROUND_Y)
    if 0 < t < min_t:
        min_t, hit_n, hit_c, hit_mat = t, n, get_ground_color(ro + rd * t), MAT_DIFFUSE
    
    return min_t, hit_n, hit_c, hit_mat
```

### 5.2 主渲染循环（迭代式）
```python
@ti.kernel
def render():
    light_pos = ti.Vector([light_pos_x[None], light_pos_y[None], light_pos_z[None]])
    
    for i, j in pixels:
        # 计算主射线方向
        u = (i - RES_X/2.0) / RES_Y * 2.0
        v = (j - RES_Y/2.0) / RES_Y * 2.0
        ro = ti.Vector(CAMERA_POS)
        rd = normalize(ti.Vector([u, v - 0.2, -1.0]))
        
        final_color = ti.Vector([0.0, 0.0, 0.0])
        throughput = ti.Vector([1.0, 1.0, 1.0])
        
        for bounce in range(max_bounces[None]):
            t, N, obj_color, mat_id = scene_intersect(ro, rd)
            
            if t > 1e9:  # 未击中
                final_color += throughput * bg_color
                break
            
            p = ro + rd * t
            
            if mat_id == MAT_MIRROR:  # 镜面材质
                ro = p + N * EPSILON
                rd = normalize(reflect(rd, N))
                throughput *= 0.8 * obj_color
            else:  # 漫反射材质
                L = normalize(light_pos - p)
                # 阴影检测
                shadow_ray_orig = p + N * EPSILON
                shadow_t, _, _, _ = scene_intersect(shadow_ray_orig, L)
                dist_to_light = (light_pos - p).norm()
                
                ambient = 0.2 * obj_color
                direct_light = ambient
                if shadow_t > dist_to_light or shadow_t < 0:
                    diff = ti.max(0.0, N.dot(L))
                    direct_light += 0.8 * diff * obj_color
                
                final_color += throughput * direct_light
                break
        
        pixels[i, j] = ti.math.clamp(final_color, 0.0, 1.0)
```

---

## 六、实验结果与分析
### 6.1 渲染效果
程序成功实现了以下效果：

1. **阴影效果**：点光源位置改变时，阴影能够实时、正确地跟随移动
2. **棋盘格地面**：地面呈现清晰的黑白相间棋盘格纹理
3. **镜面反射**：银色球体能正确反射场景中的其他物体（红色球体和地面）
4. **多弹射效果**：增加最大弹射次数后，镜面球体中能出现多次反射的细节

### 6.2 不同弹射次数对比
| 弹射次数 | 效果描述 |
| :--- | :--- |
| 1 | 只有直接光照，镜面球呈现纯色，无反光效果 |
| 2 | 镜面球出现一次反射，能看到红色球体的倒影 |
| 3 | 出现多重反射，镜面中能看到反射的反射 |
| 4+ | 反射细节更加丰富，但亮度逐渐衰减 |


### 6.3 关键技术难点及解决
| 问题 | 解决方案 |
| :--- | :--- |
| Shadow Acne | 射线起点沿法线方向偏移 $ \epsilon = 10^{-4} $ |
| GPU递归限制 | 使用for循环迭代代替递归调用 |
| 棋盘格纹理 | 根据交点坐标奇偶性动态计算颜色 |
| Taichi语法限制 | 避免在`@ti.func`中的if块内直接return |


---

## 七、实验总结
### 7.1 收获与体会
通过本次实验，我深入理解了：

1. **光线追踪的核心思想**：通过追踪光线在场景中的传播路径来模拟全局光照效果
2. **Whitted模型的局限性**：只处理镜面反射和阴影，未考虑漫反射间的光线传递
3. **GPU编程特点**：需要避免递归，使用迭代循环；注意静态类型和函数装饰器

### 7.2 可改进方向
1. **抗锯齿**：每个像素多次采样后平均，消除边缘锯齿
2. **折射效果**：增加玻璃材质，实现光线折射
3. **软阴影**：使用面光源或多点采样获得更真实的阴影
4. **加速结构**：使用BVH等空间划分加速复杂场景求交

---

## 八、附录
### 8.1 文件结构
```plain
CGLAB5/
├── .venv/              # 虚拟环境
├── config.py           # 配置文件
├── raytracer.py        # 求交和光线追踪逻辑
├── main.py             # 主程序入口
└── test.py             # 测试脚本
```

### 8.2 运行说明
```bash
# 激活虚拟环境
.venv\Scripts\activate

# 运行程序
python main.py
```

### 8.3 Git提交记录
```bash
git add .
git commit -m "完成光线追踪实验：实现镜面反射、阴影和交互控制"
git push origin master
```

---

